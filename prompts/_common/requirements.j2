- description (тип: string) — краткое описание блока (1-2 предложения)
- content (тип: string) — полная информация: код сохраняй целиком, требования — подробно
- id (тип: string) блоков не должны повторяться — они должны быть уникальны
IDшники блоков, которые уже используются в системе:
{{ blocks_ids_context }}

ВАЖНО:
- Сохраняй код, SQL, команды, формулы ПОЛНОСТЬЮ — без сокращений
- Сохраняй технические требования и спецификации ПОДРОБНО
- Не теряй важные детали при группировке — лучше один большой блок, чем потеря информации
